
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run-utam-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Install dependencies
        run: npm install

      - name: Build UTAM
        run: npm run build:utam

      - name: Run UTAM tests
        run: npm run wdio -- --spec ./src/test/features/testcheck.feature
